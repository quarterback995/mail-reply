# Mail-Reply-Agent 用户使用指南

## 快速开始

### 注册与登录

1. 打开浏览器，访问 `http://服务器IP:3000`
2. 点击 **注册**，输入用户名和密码
3. 注册成功后自动登录

---

## 功能模块

### 一、草稿工作台

用于生成邮件回复草稿。

#### 基本使用

1. 在左侧 **"新邮件"** 区域粘贴收到的客户邮件
2. （可选）上传客户附件 — 点击 **"上传客户附件"** 按钮，支持 PDF、TXT、Word、图片等文件
3. （可选）在底部输入附加说明，如"更正式一些"或"用中文回复"
4. 选择模型档位：
   - **快速**：响应最快，适合简单邮件
   - **均衡**：速度与质量平衡，推荐日常使用
   - **高质量**：最强能力，适合重要邮件
5. 点击 **"生成回复"**

#### 网页参考

在底部 URL 输入栏可以抓取网页内容作为回复参考：

**单页抓取（临时参考）：**
1. 输入网页 URL
2. 点击 **"抓取"** — 抓取单页内容，仅用于本次生成
3. 抓取成功后显示网页标题和字数

**整站爬取（保存到知识库）：**
1. 输入网站起始 URL
2. 点击 **"保存到知识库"** — 自动爬取子页面并保存
3. 按钮实时显示进度：`已访问 X 个链接` → `已保存 X/Y 页`
4. 爬取在后台运行，切换页面不会中断
5. 完成后弹窗通知

> 注意：爬取最多 50 个页面，深度 2 层。同一网站重复爬取会自动替换旧数据。

#### 附件说明

- 上传的附件会自动提取文本内容
- 生成回复时，附件内容会作为上下文一并发送给 AI
- 附件以标签形式显示，可随时删除

#### 翻译功能

每侧（原文/草稿）都支持翻译：

1. 选择目标语言（支持中、英、日、韩、法、德、西等 10 种语言）
2. 选择翻译引擎：百度 / Google / AI
3. 点击翻译图标
4. 点击 **"查看原文"** 切换回原文

#### 历史记录

- 点击右上角 **"历史记录"** 查看生成记录
- 点击任意记录可恢复之前的邮件和草稿
- 草稿区支持直接编辑修改

#### API 切换

如果后端配置了多个 API，右上角会显示下拉框，可快速切换。

---

### 二、数据中心

管理回复模板和知识库。

#### 回复模板

保存历史对话记录，帮助 AI 学习您的回复风格。

**新建模板：**

1. 点击 **"新建模板"**
2. 输入模板名称（如"客户咨询回复"）
3. 添加对话轮次：
   - **客户**：粘贴客户的原始邮件
   - **用户**：粘贴您的回复
4. 可为每条消息添加附件（支持文件上传）
5. 点击 **"保存模板"**

**编辑模板：**

1. 点击模板列表中的编辑图标（铅笔）
2. 修改内容
3. 点击 **"保存修改"**

**搜索模板：**

- 在搜索框输入关键词，按名称或内容过滤

**其他操作：**

- 预览：查看模板原始内容
- 下载：导出为文本文件
- 删除：移除模板

#### 知识库

上传参考文档（菜单、政策、FAQ 等），AI 生成回复时会自动检索参考。

**上传文件：**

1. 点击 **"上传文件"**
2. 选择文件（支持 TXT、PDF、MD、Word、图片 OCR）

**编辑文档：**

1. 点击文档右侧的编辑图标（铅笔）
2. 在内联编辑器中修改内容
3. 点击 **"保存修改"**

**重命名：**

1. 点击文档右侧的重命名图标
2. 输入新名称，按 Enter 确认

**排序：**

知识库列表上方显示排序按钮：
- **名称** — 按文件名排序
- **时间** — 按创建时间排序
- **大小** — 按文档字数排序

点击切换升序/降序（显示 ↑ / ↓ 箭头）。

**批量删除：**

1. 勾选要删除的文档（支持全选）
2. 点击 **"删除选中"** 按钮

**语义检索测试：**

知识库顶部有搜索栏，可测试 RAG 检索效果：
1. 输入关键词或问题（如"营业时间"、"退款政策"）
2. 点击 **"检索"**
3. 查看返回的相关片段，验证知识库是否能被正确召回

**爬取网站到知识库：**

在草稿工作台底部输入 URL，点击 "保存到知识库"，网站内容会自动保存到知识库供长期使用。

---

### 三、API 设置

配置 AI 模型和 API 参数。

#### 选择预配置 API

如果后端配置了多个 API，页面顶部会显示卡片选择器，点击即可切换。

#### 自定义配置

切换到 **"自定义配置"** 模式，可以手动填写：

- API 提供商
- API Key
- Base URL
- 模型名称

#### 测试连接

填写配置后点击 **"测试连接"**，确认 API 可用。

#### 保存设置

点击 **"保存设置"** 将配置保存到浏览器本地。

---

### 四、使用记录

查看历史生成记录：

- 包含时间、邮件内容、生成草稿、Token 用量
- 支持清空全部记录

---

## 界面功能

### 暗色模式

点击左下角太阳/月亮图标切换亮色/暗色模式。

### 面板调整

- 拖动左侧面板边缘可调整左右宽度
- 拖动底部面板边缘可调整底部高度
- 布局自动保存，切换页面后恢复

### 配额查看

左下角显示今日配额使用情况。

### 退出登录

点击左下角 **"退出"**。

---

## 常见问题

### Q: 生成回复失败（504 超时）？

1. 检查 API 设置是否正确
2. 邮件内容过长会导致生成时间增加，尝试缩短输入
3. 检查网络连接，国内服务器避免使用需要代理的 API

### Q: 生成回复失败（其他错误）？

1. 检查 API 设置是否正确
2. 点击 **"测试连接"** 确认 API 可用
3. 检查是否超出每日配额

### Q: 附件上传后没有效果？

附件文本会自动提取并包含在生成请求中。如果附件是图片或扫描 PDF，可能无法提取文本。

### Q: 模板编辑后没保存？

编辑模板时页面顶部会显示 **"未保存"** 标签。离开页面前会弹窗提醒是否保存。

### Q: 网站爬取没有抓到某些内容？

1. 爬取只处理静态 HTML，JavaScript 动态加载的内容无法抓取
2. 某些页面可能被过滤（如登录页、购物车等）
3. 可使用"单页抓取"先测试目标页面

### Q: 知识库搜索不到预期内容？

1. 使用语义检索测试功能验证召回效果
2. 确保文档已正确上传（查看创建时间确认）
3. 尝试不同的关键词或问题描述

### Q: 如何提高回复质量？

1. 上传更多回复模板，帮助 AI 学习您的风格
2. 上传知识库文档，提供业务背景
3. 使用 **"高质量"** 模型档位
4. 在附加说明中提供具体指导
5. 抓取相关网页作为临时参考
